SCD Health System — HTML package update

The original HTML package has been combined with:
- facility-reports.html
- print-documents.html

dashboard.html now contains a Print & Reports section with:
- Patient ID / SCC# selection
- Patient document selection
- Patient Copy / Clinical Copy selection
- Facility report selection
- Facility unit selection
- Reporting period selection

The two report pages are also available as standalone HTML pages and are
linked from the dashboard.

PHP/backend files were intentionally not created or changed in this update.
They will be handled later when the full-system PHP prompt is provided.
